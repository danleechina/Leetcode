#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <map>
#include <algorithm>

using namespace std;

class Solution {
public:
    bool isValid(string s) {
        
    }
};

int main(int argc, char const *argv[])
{
  auto sln = Solution();
  cout << sln.isValid("(((") << endl;
  return 0;
}
